# 🧠 FGK Adaptive Huffman Encoder

Реализация алгоритма **динамического кодирования FGK (Faller–Gallager–Knuth)** на чистом Python:

- ✅ Без ООП
- ✅ Без внешних библиотек (кроме `loguru` для логирования)
- ✅ Поддержка любых бинарных файлов
- 🎨 CLI-интерфейс с **цветными логами**
- 🔁 Полные `encode`/`decode` циклы
- 🧪 Автоматические тесты
- 📦 Устанавливается как Python-пакет (`pip install`)
- ⚙️ Поддержка `Makefile` для быстрых операций

> 💡 Отлично подходит для демонстрации сложных алгоритмов, учебных проектов и фриланс-заказов.

---

## 🛠️ Установка и запуск

### 1. Клонируйте репозиторий

```powershell
git clone https://github.com/mr-carleone/fgk-tool.git
cd fgk-tool
```
### 2. Создайте виртуальное окружение (рекомендуется)

```powershell
python -m venv venv
```

Активируйте его:

* Windows (PowerShell):
```powershell
.\venv\Scripts\Activate.ps1
```
* Windows (CMD):
```powershell
.\venv\Scripts\activate.bat
```

### 3. Установите зависимости

```powershell
pip install -r requirements.txt
```

### 4. Запустите тесты

```powershell
python -m tests.test_fgk
```

✅ Ожидаемый вывод:

```txt
✅ test_simple_string passed
✅ test_file_roundtrip passed
🎉 All tests passed!
```

## 📦 Установка как пакет (рекомендуется для CLI)

Благодаря `pyproject.toml` вы можете установить `fgk` как глобальную команду:

```powershell
pip install -e .
```

После этого доступна короткая команда:

```powershell
fgk encode input.txt output.fgk
fgk decode output.fgk restored.txt
```

💡 Это работает благодаря `[project.scripts]` в `pyproject.toml`.

> Команда `fgk` будет доступна в любом месте терминала, пока активно виртуальное окружение.

## ⚡ Быстрые команды через Makefile (Linux/macOS / WSL)

Если у вас установлен `make:`

```bash
make install    # pip install -e .
make test       # запустить тесты
make clean      # удалить временные файлы
```

## 🚀 Использование CLI

**Вариант 1: через модуль (без установки)**

```powershell
python -m fgk.cli encode input.txt output.fgk
python -m fgk.cli decode output.fgk restored.txt
```

**Вариант 2: как установленная команда (после `pip install -e .`)**

```powershell
fgk encode input.txt output.fgk
fgk decode output.fgk restored.txt
```

> 📁 Поддерживаются любые файлы: текст, изображения, бинарники — не только `.txt`!

## 📂 Структура проекта

```yml
fgk-tool/
├── fgk/
│   ├── __init__.py
│   ├── core.py          # чистая реализация FGK
│   └── cli.py           # CLI + цветные логи
├── tests/
│   ├── __init__.py
│   └── test_fgk.py      # автоматические тесты
├── requirements.txt     # зависимости для разработки
├── pyproject.toml       # метаданные пакета + скрипты
├── Makefile             # удобные команды (опционально)
├── .gitignore
└── README.md
```

## ⚠️ Важно

* 📉 Алгоритм не предназначен для практического сжатия — на коротких данных он расширяет файл из-за overhead на передачу новых символов.
* 🧩 Реализация учебная: дерево строится динамически, но без оптимизации весов через swap (упрощено для работы без ООП).
* 📦 Настоящие архиваторы (например, `gzip`, `bzip2`) используют гораздо более эффективные методы.

## ✅ Проверка работоспособности

```powershell
# Активируйте venv (если не активирован)
.\venv\Scripts\Activate.ps1

# Установите пакет (рекомендуется)
pip install -e .

# Протестируйте
fgk encode README.md test.fgk
fgk decode test.fgk test_restored.md

# Убедитесь, что файлы идентичны
fc README.md test_restored.md
```

> 💬 Совет: для точного сравнения используйте побайтовую проверку через Python:
> `python -c "print('✅ OK' if open('a','rb').read() == open('b','rb').read() else '❌ FAIL')"`

## ✨ Приятного использования! Если проект помог — поставьте ⭐ на GitHub!

## 📝 Лицензия

### 📄 MIT — используйте свободно в личных и коммерческих целях.
